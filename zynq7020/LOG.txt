26.3.12 | update | 1.更新了README，加入了核心文件功能说明
2.重构了项目架构，采用app mid bsp hal四层的嵌入式开发标准架构，只允许上层调用下层，不允许下层调用上层（gui算app层但解耦界面与业务）
对于文件夹的部分使用了ide的c++ include path，这样以后可以不用再使用复杂的相对路径，直接用文件名include即可，不过要注意如果加入了新的文件夹要在includepath里加入，具体方法见README  
3.将上帝类main.c解耦，现在的main.c非常精简，要改业务逻辑和ui只需要去对应的文件修改即可
4.剔除了早期用于测试的模拟器运行逻辑与冗余注释，重新优化了控件尺寸，使其成为一个干净的、完全由数据驱动的纯视图层


