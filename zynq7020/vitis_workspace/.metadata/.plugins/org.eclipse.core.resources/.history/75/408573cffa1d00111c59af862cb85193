// app/app_main.c
#include "app_main.h"

/* 包含底层所有大厨和服务员的头文件 */
#include "platform.h"
#include "lcd_drv.h"
#include "sys_timer.h"
#include "zynq_interrupt.h"
#include "adc_dma.h"
#include "ui_main.h"
#include "lvgl.h"

// 假设这些在你的网络文件里
extern void setup_udp_connection(void);
extern void xemacif_input(struct netif *netif);
extern struct netif *echo_netif;

void Hardware_Init(void) {
    init_platform();           // 开启Cache等
    LCD_Init_Hardware();       // 屏幕底层点亮
    System_Timer_Init();       // 开启定时器

    // 初始化中断和DMA (原 main.c 里的相关逻辑搬到这里)
    // InterruptInit(...);
    // XAxiDma_Initial(...);
    // ...
}

void UI_Init(void) {
    lv_init();

    // LVGL 的显示驱动注册大段代码搬到这里
    // lv_disp_draw_buf_init(...);
    // lv_disp_drv_init(...);
    // lv_disp_drv_register(...);

    // 调用真正的界面绘制
    my_gui_init(); // 对应 ui_main.c 里的初始化
}

void Network_Init(void) {
    // lwip_init();
    // xemac_add(...);
    // setup_udp_connection();
}

void Network_Task(void) {
    // xemacif_input(echo_netif);
}

void LVGL_Task(void) {
    lv_timer_handler();
}
