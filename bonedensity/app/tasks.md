# 当前任务

本文件是Agent执行长任务时的即时记忆，记录当前任务的范围、步骤、测试、验收和检查点。任务过程中持续更新；完成后只保留简短结果和最近任务索引。维护规则见 `AGENTS.md`。

## DOC-001：统一项目文档体系

对应需求：`REQ-011`

状态：待用户验收

### 目标与范围

- 建立 `REQUIREMENTS.md`、`PROJECT_CONTEXT.md`、`PROJECT_PROGRESS.md`。
- 重写 `tasks.md` 和 `AGENTS.md`，精简 `CLAUDE.md`。
- 迁移后删除重复的计划、周报、进度和重构日志。
- 不修改业务代码、资源或四张下一任务背景图。

### 实施步骤

- [x] 盘点全部Markdown文档及Git历史。
- [x] 与用户确认四层文档职责和维护流程。
- [x] 建立需求、上下文和人类进度文档。
- [x] 将本文件改为当前任务和执行检查点。
- [x] 审计并重写 `AGENTS.md`，精简 `CLAUDE.md`。
- [x] 删除内容已迁移的重复文档。
- [x] 检查文档结构、链接、需求与任务对应关系。
- [x] 检查最终Git差异并准备独立提交。

### 测试与验收标准

1. 每项已实施或计划中的主要功能都能在 `REQUIREMENTS.md` 找到状态和产品预期。
2. `PROJECT_CONTEXT.md` 足以让新Agent了解构建、边界、数据、当前流程和现场未验收项。
3. `PROJECT_PROGRESS.md` 按周从近到远排列，语言简短，不包含详细测试步骤。
4. `tasks.md` 包含当前范围、测试、验收、状态和最近检查点，并关联需求编号。
5. `AGENTS.md` 明确文档维护、自动测试迭代和五轮失败停止规则，且没有重复或冲突要求。
6. 重复日志删除后，专项技术文档继续保留，Markdown内部引用均能找到目标。
7. 最终Git差异只包含文档整理；四张年龄-SOS背景图保持未跟踪且不纳入提交。
8. 本任务只修改Markdown，不需要Qt构建；以内容检查、链接检查和Git差异作为验证。

### 实际验证结果

- 已确认本地与远程分支同步，整理基线为 `849b1f5`。
- 已检查全部Markdown文件，没有指向已删除日志的遗留引用。
- 已检查需求编号、当前任务关联、上下文章节和周报日期结构。
- `git diff --check` 通过，无尾随空格或格式错误；未修改任何业务代码或资源。
- Qt构建未执行：本任务只修改Markdown，构建不能增加有效覆盖。

### 当前检查点

- 远程分支与本地完全同步，基线提交为 `849b1f5`。
- 四张未跟踪BMP已确认是下一任务素材，本任务不处理。
- 文档迁移、规则审计、旧日志删除和最终差异检查已经完成，等待独立提交和用户验收。

### 阻塞问题和用户决定

- 用户确认采用四层文档加 `AGENTS.md` 执行规则。
- 用户确认人类周报默认只写已验证的重要结果，未验收时才特别标记。
- 用户确认Agent先自测迭代；同一问题连续五轮仍失败时停止并说明情况。

## 后续任务队列

- `REQ-008` 年龄-SOS参考图：已确认，但用户要求暂停，等待重新开始。
- `REQ-009` 标准试块校准：先完成D值和温度补偿依据调研。
- `REQ-010` 整机数据迁移：审计账号、患者、历史、校准配置和资源依赖。

## 最近完成任务索引

- `REQ-007` 样式化检测报表：功能已实现，真实五轮自动跳转和实体打印待实验室验收。
- `REQ-006` 响应式主界面：用户已确认默认窗口显示正常。
- `REQ-001` 至 `REQ-005`：账号、患者历史、检测流程和USB稳定性，详细状态见 `REQUIREMENTS.md`。
