# 当前任务

本文件是Agent执行长任务时的即时记忆，记录当前任务的范围、步骤、测试、验收和检查点。任务过程中持续更新；完成后只保留简短结果和最近任务索引。维护规则见 `AGENTS.md`。

## AGE-001：年龄-SOS参考图

对应需求：`REQ-008`

状态：待用户验收

### 目标与范围

- 将主界面中间上方空白区域改为年龄-SOS参考图。
- 按患者性别和检测时年龄选择中国男性、女性、男童或女童素材。
- 只标记当前患者最新一次检测的年龄和SOS。
- 不修改信号处理、SOS计算、T/Z值、诊断、门限、串口协议或采集时序。

### 已确认规则

- 男童/女童素材年龄范围为0至20岁；男性/女性成人素材范围为20至100岁。
- 0至19岁使用儿童图，20岁及以上使用成人图。
- 最新记录优先使用保存的患者性别和年龄快照；旧记录缺失时根据患者生日和检测日期计算。
- 无检测记录时根据当前患者资料显示背景但不显示点；无患者时显示中性空状态。
- 缺失或超范围数据不得伪造点位，也不得把点强行限制在图边缘。
- 图片保持原始比例缩放，不裁切、不拉伸。

### 实施步骤

- [x] 审核四张BMP尺寸、年龄范围、SOS刻度和绘图区坐标。
- [x] 新增独立年龄-SOS绘图控件并注册四张资源图。
- [x] 将主界面原空白参考区替换为新控件并接入响应式布局。
- [x] 从当前患者和最新检测记录生成性别、年龄、SOS显示数据。
- [x] 在患者选择、清空、历史更新和检测完成后刷新参考图。
- [x] 完成Debug clean build和无硬件运行测试。
- [x] 更新项目上下文和人类进度，检查差异并准备独立提交。

### 测试用例

1. 女童、男童、成年女性、成年男性各一名：背景图与性别和年龄一致，点位在对应年龄和SOS位置。
2. 19岁使用儿童图，20岁使用成人图。
3. 无患者：显示中性空状态，不选择任意人群背景。
4. 已选择患者但无记录：显示对应背景，不显示红色测量点。
5. 同一患者有多条记录：只显示按检测时间排序的最新记录。
6. 新格式记录使用保存的年龄快照；旧格式记录根据生日和检测日期计算年龄。
7. 性别、生日、年龄或SOS无效，以及年龄/SOS超出素材范围：不绘制虚假点，并显示简短状态。
8. 默认窗口、最大化及缩小窗口：图片保持比例，坐标点和标签不被裁切，其他主界面区域不被遮挡。
9. 运行 `build-debug.ps1` 后启动程序，不出现MSYS入口点错误；匿名边界数据保存在 `testdata/age-sos-reference/`，运行时副本与真实档案隔离。

### 验收标准

- 选择患者后背景正确，最新检测点位置与图片坐标一致。
- 切换患者、修改档案或新增检测记录后立即刷新。
- 无结果和异常数据不生成虚假点。
- 所有窗口尺寸下图像清晰、完整、无拉伸和遮挡。
- Git差异不包含受保护算法、串口和门限修改。

### 实际验证结果

- Debug clean build通过，固定Qt/MinGW工具链生成并部署可运行程序；仅有项目原有编译警告。
- 隔离XML实际运行验证女童、男童、成年女性、成年男性四类背景，19岁使用儿童图、20岁使用成人图。
- 同患者两条记录只显示最新SOS；旧记录缺少年龄快照时按生日和检测日期得到正确年龄。
- 无患者显示中性状态；无历史记录显示对应背景和“暂无检测结果”，不绘制红点。
- SOS超出素材范围时显示明确提示，不将点压到边缘。
- 最大化和默认窗口下图片保持比例，主界面无新增遮挡；右上角点位标签已避开素材图例。
- 隔离测试档案按用户要求保留用于演示，不覆盖正常运行目录数据，也不提交Git。
- 已将可复现的匿名边界数据整理到 `testdata/age-sos-reference/`；运行时隔离副本继续保留在本机，不纳入Git。

### 验收证据与复现

- `G19`、`W20`：验证19岁使用儿童图、20岁使用成人图。
- `B12`、`G19`、`W20`、`M45`：验证四类性别和年龄背景。
- `M45`：包含两条历史，验证只标记最新的4100.0 m/s。
- `OLD16`：旧记录无年龄快照，验证按生日和检测日期回算16岁。
- `EMPTY`：验证无历史时只显示背景和“暂无检测结果”。
- `OUTSIDE`：5000.0 m/s超出男性成人图范围，验证提示超范围且不绘制假点。
- 复现时将 `testdata/age-sos-reference/` 中两份XML复制到独立的已部署程序目录，启动后使用档案导入依次选择上述患者；不得覆盖真实实验目录。

### 当前检查点

- Agent自测已完成，等待用户验收。
- 四张素材改用ASCII资源文件名以兼容qmake，图片内容未改变。

### 阻塞问题和用户决定

- 用户确认开始本轮任务。
- 默认只显示最新一次检测，不叠加历史点。
- 未选择患者时使用中性空状态。

## 后续任务队列

- `REQ-009` 标准试块校准：先调研D值、标准流程和温度补偿依据。
- `REQ-010` 整机数据迁移：先审计存储；若校准数据会增加迁移困难，则优先完成迁移基础。

## 最近完成任务索引

- `REQ-011` 文档治理：已建立需求、任务、上下文和简短周报规范。
- `REQ-007` 样式化检测报表：功能已实现，真实五轮自动跳转和实体打印待实验室验收。
- `REQ-001` 至 `REQ-006`：账号、患者历史、检测流程、USB提示和响应式布局，详细状态见 `REQUIREMENTS.md`。
