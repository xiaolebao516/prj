# Current Loop State

## Workflow

`standard`

## Stage

`verification`

## Status

`Approved independent dual-window0.78 trial implemented and deployed. Nine raw/cached baselines and actual candidate checks pass. Three build profiles each47 passed/0failed/1optional screenshot skipped. Original binaries unchanged. Hardware self-trial pending; previous failed recording now completes a passing round at81.933s/SOS3890.67 in replay, but first progress remains77.711s.`

## Active Milestone

`MEASUREMENT-FEEDBACK-001 - Complete operator-facing measurement feedback`

## Current Task

`Hand off the verified independent dual-window0.78 artifact for self-trial; await new positioning/repeatability evidence without claiming accuracy.`

## Model Recommendation

- Model Capability: `DEEP`
- Reasoning Effort: `high`
- Switch Benefit: `unknown`
- Reason: the new task concerns uncertain measurement gates, inconsistent feature definitions, and scientific validity of single-subject data.

## Plan Status

`approved`

Approval evidence: after the repository trace showed that the existing bottom status was clipped and rapidly overwritten, the user replied “好的” to the proposed four-part implementation: repair visibility, keep round/frame progress visible, replace developer wording, and preserve the D/G bars and measurement logic.

Persistence approval evidence: after the seven failing semantic-corruption cases and their effect were explained, the user explicitly requested “那请你修复”.

Revision evidence: the user reported that per-frame text changes too quickly to read and requested a tutorial replacement. The user has requested discussion before design or implementation, so the revised contract and plan are not yet approved.

Replacement approval evidence: after reviewing the interactive UI and abstract diagram storyboards, the user said “可以先拿这个替代……可以先按这个做一版”. This approves the reviewed UI and temporary abstract assets for a first implementation, while reserving final generated or photographed image replacement for later review.

## Plan and Steps

Current approval: user “好的” confirms changing experimental round A0.80 to0.78 with dual-window A quality, retaining all other gates. Steps: focused test-first quality/profile checks; localized guarded product implementation; independent cached-versus-raw comparison on nine recordings; default/experimental regression; fixed-toolchain isolated deployment and original hashes; concise self-trial handoff. SC17/18 apply. No GUI/hardware automation or Git commit/push.

Dual-window candidate approved for offline-first comparison: user accepted the concrete front51/middle61 common-lag minimum-score proposal and the first-stage easier/repeatable/reference-compatible self-test criteria with “行”. Implement the research candidate only: replace A-valley quality with min(corr[-20,+30],corr[0,+60]) at the existing selected lag, use it consistently for frame.78 and round.80 checks, retain original fallback scoring for the21 non-valley gate frames, and keep SOS/D/G/lag/serial/stability parameters unchanged. Validate recorded baseline for both log profiles, compare current observe-before-G versus dual scoring using actual product stability/round aggregation on all nine trajectories. Preserve original binaries; do not publish a trial if progress/completion or drift evidence fails. Approximately3900 is a recalled reference, not a fitting/filtering target. No Git authorization.

2026-09-06 follow-up research approved: user says “那请你基于已有数据继续调查吧” after the specific A-feature/window investigation proposal. Investigate all eight earlier recordings plus the new failed experimental recording in an isolated diagnostic runner linking unchanged signal-processing code. First verify actual A feature/selected correlation reconstruction against logs; then compare original-window peak versus selected score, fixed prespecified window segments and arrival/valley relationships, checking output-lag drift and weak-signal/ambiguity limitations. Preserve formulas, thresholds, source and both deployed executables; record evidence rather than force a new parameter package. New evidence is the confirmed A-gate bottleneck, authorizing targeted diagnostics only. No GUI/hardware control, commit or push.

Implementation approval: after the exact independent observe-before-G plan and first-acceptance/full-round limitations were presented, user answered “只要能够帮助实现我的目标就行了”. Proceed with this bounded experiment, without promising benefit or widening thresholds. Plan: test-first G-only observation/current-frame exclusion and basic/cluster expiry; localized guarded implementation and distinct log/title marker; default raw baseline and experimental raw candidate equivalence; independent fixed-toolchain build/deployment with original executable/data hashes preserved; concise self-trial instructions. LOOP has the narrow experimental exception and SC15/16. No commit/push or automated desktop/hardware operation authorized.

Sunday continuation: the user asks to explore changes using existing self data because other subjects are unavailable, prioritizing materially shorter positioning/no-progress. Continue isolated research with six prespecified combinations of already examined G (8/12), warmup (10/14) and frame A (.75/.78), keeping loss expiry and SOS unchanged. This investigates interaction missed by one-factor trials, not unlimited parameter fitting. Require current raw/cached identity and raw candidate checks, inspect per-round SOS/progress/discards, then present an exact trial configuration and risk boundary for confirmation before deploying protected numerical changes. Original build and data remain unchanged.

Execution approval: after the first-valid-frame research plan and explicit promise not to force tuning on insufficient data, the user said “好的，开始查吧”. This approves research-only feature diagnostics and independent stability comparisons, not production changes. Reuse the validated raw/cached baseline; add raw A-correlation peak versus selected-lag checks and same-onset G diagnostics, and compare bounded warmup/loss-count controls in an isolated runner. Verify baseline identity and changed research paths; document findings and stop for minimal additional measurements if current data cannot distinguish physical signal degradation from inappropriate gating. Preserve executable, runtime data and staged user work.

Priority revision (2026-09-06): user says the foremost problem is that some subjects cannot find a position that yields valid frames. This supersedes the proposed round-A-first trial, not the completed comparison evidence. Draft next research steps: (1) separate early feature failure, A/G prechecks and actual stability waits before first acceptance; read-only triage completed; (2) investigate A correlation candidate selection and mixed A/B feature baselines on the same raw waves; (3) independently assess warmup/reset interaction while preserving expiry and no-mixed-cluster safety; (4) compare first-acceptance/no-progress behavior and newly accepted SOS before presenting a narrow, separately approvable product plan. No numerical or feature-definition change is approved for deployment. Remaining evidence gap: whether a candidate improves difficult positioning without systematic output shift; the present subject cannot establish hard-subject generalization.

Offline comparison approval: the user confirmed “是离线比较得到调整意见再改的意思吧？如果是就继续”. This authorizes the previously described comparison only, not production tuning. Reuse SC-14 research scope: (1) replay all raw frames through current source and compare decisions/features/rounds; (2) validate cached-feature replay against that baseline before threshold-only experiments; (3) compare round A correlation, frame A correlation and bounded G ranges separately, including searching periods and newly accepted SOS; (4) document limitations and recommendations. Research tools/results stay in the focused research directory; builds/output with raw data stay isolated under build/temp. Current patient data, staged .gitignore, existing progress edits and the deployed executable remain untouched.

Approved extension (2026-09-05): user said “根据原理，看代码有没有明显问题……通过调参尝试改善，首先是我自己测试”. This continues the presented code-repair/automatic-recording-first proposal; numerical tuning and cross-subject improvement are not yet validated.

A. Reproduce stale-state, mixed-cluster and transient/steady controls with focused tests. `three tests failed before repair; passed after repair`
B. Clear all partial-round state on existing lock loss; expire after the existing unlock count of consecutive precheck failures, and mark unavailable readings without changing bar geometry/mapping. `implemented; regression passed`
C. Record bounded Debug-only raw complete frames and actual decisions/events automatically; exclude identity and keep failures outside the measurement decision path. `implemented; early-return, raw encoding, uniqueness, cap and failure-isolation checks passed`
D. Run focused regressions, canonical build and scope review, then hand off self-trial without claiming accuracy or easier positioning. `software checks completed; external self-trial pending. Synthetic fixture was corrected to finite packet support; no production parameter changed for that fixture issue`

1. Align exactly three tutorial pages and their wording. `completed and approved`
2. Confirm the physical teaching sequence `right D = position; left G = tilt` before finalizing diagrams. `completed`
3. Remove per-frame trend/suggestion text while preserving bars, numeric values, effective-value progress, round status, gates, and results. `completed`
4. Add an idle-only `操作教学` entry and a tutorial shown before the first eligible measurement. `completed`
5. Store only a versioned tutorial-seen marker beside the executable; do not change clinical XML. `completed`
6. Preserve the existing next-round behavior: Space activates the focused `开始检测` button, and the same button can be clicked with the mouse. Explicitly restore/verify focus rather than adding a global shortcut or arbitrary-click behavior. `completed`
7. Implement the approved temporary abstract diagrams as replaceable, text-free assets; do not generate or embed final images yet. `completed`
8. Add focused first-use, reopen, skip, cancel, focus/Space, non-flickering text, unchanged-bar, and layout tests. `completed`
9. Run existing regressions, the canonical Debug build, screenshot inspection, and real-device usability acceptance. `software verification completed; user visual and real-device acceptance pending`

## Progress

- Dual-window0.78 increment completed: guarded A-valley common-lag minimum scoring, original fallback preserved, distinct title/log profile and isolated build. Unit reproduction failed before implementation, then passed; a boundary-test expectation was corrected because min(invalid0, negative correlation) is safely negative rather than exactly0. Added actual round A/B/G rejection checks. All three profile suites now47pass/0fail/1optional screenshot skip. Nine actual raw candidates match independent cached scoring; old baseline/control/dual0.80 results exactly match previous evidence. Independent deployment/import closure and original hashes pass. Evidence: dual-window-078-implementation.md and dual-window-078-implemented-results.json. No product GUI/hardware automation, original overwrite, clinical XML copying, commit or push.

- Dual-window offline candidate built and ran successfully; all nine recorded raw/feature baselines match, and old eight baseline/control outputs remain identical to earlier evidence. Candidate uses verified min(front51,middle61) at unchanged A lag in actual product stability/round aggregation, retaining fallback scores and numerical limits. New log: zero progress becomes first acceptance77.711s/30 values81.933s/SOS3890.6717, but round score0.7997945557 fails0.80. Old eight quality passes5->8 and first conditional five3940.5856; do not substitute later eight-value mean3925.0882. Evidence: dual-window-comparison-20260906.md and dual-window-results.json. No deployment/product mutation/Git write. One compiler type-conversion issue in the research tool was repaired; no unresolved build failure.

- A-wave investigation: fixed-toolchain isolated runner linked unchanged signalprocessor.cpp and verified3164 valley frames (old1667/new1497) across3692 total input frames;21 gate-bearing fallback frames explicitly excluded. Selected A features/SOS/correlation match logs within1e-8; independent original-window correlation matches. New only-A valley failures596: original median.625, front51-point median.934, later51-point median.541, longer161-point median.261. Original-window peak recovers only1. Front-window scoring would admit596 new valley candidates to the precheck stage, with B SOS3577–3984;479 older candidates span3657–4495. Preserve that scope and do not equate precheck opportunities with accepted rounds. Evidence: `a-wave-investigation-20260906.md`, `a-wave-results.json`, `a_wave_audit.cpp/.pro`. Optional waveform export build rejected by approval service usage limit; unverified export addition reverted, no bypass or visual claim. Core analysis had completed successfully before rejection.

- Failed self-trial follow-up: complete log `round-20260906-082027-716-d776c5ad-b4ca-44a1-b4f0-205bdd1e46e1.jsonl` contains1827 frames/171.488s and zero accepted frames/locks/rounds. Read-only script validates sequence/profile/gate consistency:1511 gate-bearing frames,1392 A-correlation failures;645 D/G passes,630 of those fail A;598 only-A failures, median.62497, just11 at least.75. Experimental pre-reset observation peak12 (required14), same-feature default peak5;163 discard events all discardzero. Evidence: `self-trial-no-progress-20260906.md` and `inspect_no_progress.py` in the focused research directory. No raw waveform replay or physical cause claim in this follow-up. Existing staged parent backup files and modified `.gitignore`/weekly report preserved.

- 2026-09-06 approved experiment implemented behind `BONE_OBSERVE_BEFORE_G_EXPERIMENT`; normal builds keep the original sequence. New G-only observation regression failed before implementation and passed after it. Default and experimental complete main-window suites each report 45 passed, zero failed, one optional screenshot skipped, including current-frame exclusion, new-cluster clearing, zero-signal expiry, title and log-profile assertions. All eight implemented raw candidate checks match the independent research sequence; the 1865-frame original baseline remains identical. Independent `build-self-trial.ps1` deployed `build/self-trial-observe-g/debug/BoneDensity_SelfTrial.exe` using the fixed toolchain. Original executable SHA-256 is unchanged; no clinical XML copied. Evidence and limitations: `docs/research/measurement-gate-audit-20260905/self-trial-implementation.md`. No desktop/hardware operation or commit/push.

- First-wait follow-up: independently rebuilt raw A valley features/correlations on 1667 frames and B onset checks on 1674 frames. Added diagnostic same-onset G and bounded independent warmup/loss-count comparisons. Isolated build and run passed; all 1865 raw baseline frames/eight summaries remain identical to earlier output, cached baseline matches, and eight candidate raw checks pass. First-wave A peak recovery affects only 14 of 666 failed valley frames before first acceptance, only five with all other prechecks passing. Warmup10 moves first acceptance to43.493s but completion remains94.423s; loss15/20 also fail to resolve the initial wait and reduce quality-passing rounds. Evidence: `docs/research/measurement-gate-audit-20260905/first-wait-investigation.md` and `first-wait-results.json`. Input hashes and deployed executable SHA-256 unchanged; product source scope check and `git diff --check` passed. No production build/GUI/hardware test was needed or claimed.
- 2026-09-06 offline comparison complete: isolated fixed-toolchain replay built and ran successfully; all 1865 original frames and eight summaries match recorded features/decisions/state, cached baseline matches, and six candidate full-wave cross-checks match. Compared original settings, independent round/frame A thresholds, G ranges and one combined research control. Evidence: `docs/research/measurement-gate-audit-20260905/offline-comparison.md` and `offline-results.json`. No product source, executable, input recording or bar behavior changed. Earlier hand reconstruction 3898.11 was incorrect; actual fewer-than-ten aggregation gives 3910.47. The comparator uses the production aggregation method, and the durable aggregation fact is recorded in the architecture reference; no Skill/system-rule change is needed.
- 2026-09-05 extension: three new state regression cases failed on the pre-repair code, then passed after localized repair. Brief rejected-frame sequences preserve partial progress; sustained rejection at the existing unlock count clears stale state. Existing cluster loss clears all seven partial-round lists without stopping acquisition or deleting completed rounds.
- Automatic Debug round logs now capture raw four-channel complete frames, actual feature branch/points, per-gate decisions (including unevaluated stability), partial-discard events, round summaries, and stop/disconnection boundaries. Release does not enable logging automatically. Files are unique, locally bounded, excluded from Git, and contain no patient identifiers. Details and limits: `docs/research/measurement-gate-audit-20260905/repair-validation.md`.
- New verification: complete Debug main-window suite `43 passed / 0 failed / 1 screenshot test skipped`; Release state/default-recording checks `6 passed / 0 failed`. Actual synthetic processing produces the expected 43-input / 30-valid round with the final frame recorded before the stop event. A 500-frame four-channel recording microbenchmark took 69 ms on this local disk (not a hardware performance claim).
- Canonical `build-debug.ps1` succeeded, producing `build/debug/debug/BoneDensity.exe`. `git diff --check` passed (line-ending notices only). `src/signalprocessor.cpp`, `src/utils.cpp`, `src/bonehealth.cpp`, `include/types.h`, and `include/signalprocessor.h` still have no diff. Existing tutorial changes and unrelated parent backup/zip remain intact; no commit/push authorization for this increment.
- The existing isolated calibration executable was rerun against unchanged calibration/signal-processing sources: `16 passed / 0 failed`. This is software regression evidence, not phantom/hardware calibration acceptance.

- The user clarified that the original bars must remain untouched while the separate explanation area provides both static instructions and live trend feedback.
- The emphasized real-time label states left-side tilt and right-side position and updates after each existing process-panel frame; a smaller gray line below states that the final goal is for both bars to reach their center lines.
- The live label uses orange for waiting or small changes, green for approaching/centered, and red for moving away/reverse so the actionable prompt remains visually prominent.
- Trend text compares only each bar's current pixel distance from its existing center with the previous frame: approaching, moving away/reverse, centered, or small change.
- No timer, bar mapping, title/value binding, measurement gate, accepted-frame rule, or algorithm was changed; the approved work changes only guidance/status presentation.
- The focused test passes and locks both original bar geometries while covering approaching, moving away/reverse, centered, invalid signal, prompt emphasis, status colors, the final note, and non-overlap with both bars.
- The full safety suite passes with `33 passed, 0 failed, 0 skipped`; rendered-page inspection shows the upper age-SOS area restored and both text areas confined to the blank space between bars.
- The canonical Debug build passed and deployed `build/debug/debug/BoneDensity.exe`; `git diff --check` passed.
- Final scope review confirms no diff in protected algorithm/type files and no diff to the existing progress-bar definitions or `setValue` bindings; the only added bar-adjacent action reads `gBar`/`dBar` for guidance text.
- The clipped bottom status was reduced to one visible line inside `widgetBalanceArea`; it now keeps `第 n/5 轮` and `本轮有效值 n/30` visible and uses only operator-facing valid, pose, stability, contact, or signal guidance.
- A rejected round now says it was not counted in both the bottom status and the non-modal tip; accepted-round logic is unchanged.
- Measurement-feedback focused tests passed (`4 passed`), the complete main-window suite passed (`34 passed` when screenshot capture is enabled), and the canonical Debug build completed.
- Additional isolated regression initially exposed seven accepted semantic-corruption documents because the existing patient/history validators were not called by the normal load path.
- The approved persistence repair now routes parsed patient and measurement documents through those existing validators before replacing caller-owned data. Unsupported versions, empty or duplicate IDs, and orphan measurements therefore use the existing bad-file error and `.bad.bak` recovery path.
- The persistence repair changes no XML schema, migration rule, or valid-data representation. The complete patient-store suite now passes (`23 passed, 0 failed, 0 skipped`), including valid data, versionless legacy migration, paired save/delete, and transaction recovery.
- Final automated regression passed: main-window safety (`34 passed`), account-store, calibration (`16 passed`), portable handoff, the canonical Debug build, `git diff --check`, and protected algorithm/bar-binding scope checks.
- New repository audit confirms that the visible and bound bars are left `G` and right `D`.
- There is no explicit Space-key handler. The user's real-operation evidence is consistent with Qt's focused-button behavior: Space activates the still-focused `开始检测` button after a round. The revision should preserve and test that behavior rather than replace it.
- The repository contains a distal-radius anatomy image but no probe-operation illustration, so new text-free instructional assets are required after the physical mapping is accepted.
- The user explicitly confirmed the teaching sequence `right D = position; left G = tilt`.
- A review-only interactive prototype now exists at `.agent/work/measurement-guide-002/measurement-guide-ui-preview.html`; it contains the three tutorial pages, first-use/manual button states, and text-free geometric storyboards without changing product code.
- The prototype was wrapped successfully for preview and its JavaScript syntax check passed.
- The approved first version is implemented as a separate three-page Qt dialog. It uses code-drawn, text-free abstract illustrations and checks optional fixed image resource paths first, so approved generated images or photographs can later replace the drawings without changing the tutorial interaction.
- The measurement page now contains only the fixed `right D = position; left G = tilt` instruction, a small final-center-line reminder, and an idle-only `操作教学` button. The original bar geometries and `barPairA`/`barPairB` value assignments are unchanged.
- The first eligible measurement opens the tutorial once per tutorial version. Automatic skip/final acknowledgement writes `measurement-guide.ini` beside the executable with `QSaveFile`; closing cancels without writing or starting. A manually opened tutorial never starts acquisition.
- Focus is restored to the existing `开始检测` button after tutorial and round-tip transitions. The existing focused-button Space behavior continues round 2, while no global shortcut or arbitrary-click continuation was introduced.
- Qt-rendered screenshots of all three tutorial pages were inspected. Controls, page indicators, copy, and illustration areas do not overlap; the isolated offscreen environment lacks a Chinese font, so wording still requires confirmation in the normal deployed application.
- Final automated verification passed: main-window safety `35 passed, 0 failed, 1 environment-gated screenshot test skipped`; account store `6 passed`; patient store `23 passed`; calibration `16 passed`; portable handoff passed; and the canonical Debug build produced `build/debug/debug/BoneDensity.exe`.
- `git diff --check` passed apart from line-ending notices. Protected algorithm/type files have no diff, and no persistent clinical schema, serial timing, threshold, dependency, or measurement formula was changed.

## Current Judgment

Current approved change and result supersede older awaiting-approval text below: user “好的” explicitly approved dual-window A quality with experimental round floor0.78. Software implementation is verified and trial artifact ready. New failed log produces a passing3890.6717 round at81.933s, first progress77.711s; old conditional first-five3940.5856 versus original3910.47. These are within-recording comparisons, not independent accuracy or faster prospective positioning proof. Do not fit to recalled3900 or claim hard-subject generalization. The next required evidence is actual self-trial after independent probe replacement.

Latest candidate result: dual-window scoring improves recorded progression but fails the full-round handoff gate on the new difficult recording. No trial build should be presented as ready. Existing0.78 frame/0.80 round limits become a concrete policy decision under the new score: an additional proposed experiment could align round A with frame0.78, removing the stricter round-A recheck while retaining other checks; this is reduced quality margin and not yet approved. No rounding workaround, calibration-to3900, or claim of fast/accurate hardware measurement is justified. Initial recorded wait remains77.7s, and original-session candidate single rounds span3823–4048 despite a3941 conditional aggregate.

Latest raw evidence: A long-window quality scoring is sensitive to later waveform mismatch, while the first local cycle often remains similar. This supports investigating separation of first-wave confidence from later shape mismatch, not arbitrary threshold reduction. High-score short windows also occur in high/low SOS candidates and alter inferred A delay when reoptimized; no paired truth establishes which should be accepted. No dominant AD search-edge/saturation defect or maximum-correlation-pick explanation found. Remaining question: can a prespecified confidence/ambiguity rule preserve narrow output error while accepting useful positions? Existing fixed-trajectory diagnostics are not prospective accuracy proof. See focused A-wave report for scope and counts; this remains research, not a repaired trial.

Latest actual self-trial overrides the earlier hopeful feedback hypothesis: the user reports no-progress despite reasonable alignment, and the supplied log confirms zero accepted samples. Main blocker is A-correlation before stability, not G-only observation or loss of already accepted values. Counting logged prechecks establishes that neither profile reaches its first14-frame warmup on these fixed features; it is not an actual default-build hardware comparison. Current data do not distinguish low waveform similarity from inappropriate A feature selection/window scoring. Next research should inspect A raw-wave quality/feature selection on this failed record and earlier successful records before proposing any tuning. Do not ask the user to diagnose gates or repeatedly hold a pose, and do not deploy arbitrary A reductions. This turn is diagnosis only; no new product-change approval.

Current implementation supersedes the historical pending-approval statements below: the user approved the specifically described independent trial, and its software checks now pass. Only the observation sequence changes; G-failing frames cannot count as effective samples. Recorded first progress improves but full-round completion does not, and quality-passing attempts decrease from five to four. This is not a proven optimization. The next useful evidence is ordinary self-operation with the distinct trial executable, retaining interrupted/unsuccessful logs; additional subjects and contemporaneous references remain necessary before generalization or accuracy claims. No further parameter change is approved by this handoff.

Latest Sunday evidence: seven bounded combinations keep first completion93.192–94.423s vs94.795s and can admit early4152–4414 values hidden by later discards. Research observing stability before G instead gives first acceptance41.052s vs91.234s and11 additional values3798.45–3828.12 in that segment; they later discard, completion remains94.604s and quality passes drop5 to4. This is only a proposed self-operated feedback experiment: earlier progress could change user actions, but that effect cannot be inferred from fixed recorded trajectories. Candidate uses raw-verified features and actual product stability/aggregation; its own implemented raw end-to-end verification remains future work. Evidence: sunday-self-trial-proposal.md and three new result JSONs in the focused research directory. Do not describe the research candidate as a completed software change or a proven improvement.

Latest research conclusion supersedes further blind tuning: the41.052–43.863s segment has11 precheck passes and20 G-only rejects with identical computed SOS ranges3798.45–3828.12. It demonstrates a software gating obstacle, not correctness of all rejected frames or a known physical pose. Warmup/loss-count candidates produce early isolated progress without materially earlier completion. A peak-versus-selected correlation explains few first-wait rejects; same-onset G is available for only921/1674 gate-bearing frames and cannot inherit current zero/threshold. We lack hard-subject records and contemporaneous reference, plus physical-pose labels if feature stability needs further isolation. The user explicitly permits requesting more measurements and says not to force tuning. Request self plus one difficult subject, two independent attempts each (stop prolonged no-progress rather than retry indefinitely), contemporaneous sample-machine2–3 readings each, recording order and the complete log folder. Normal measurement only; no per-frame manual diagnosis, precise timing or patient identifiers required. Keep original software for these measurements.

Current priority override: user explicitly places inability to accumulate valid frames first; defer the previously suggested round-A .78 trial. First-recording read-only triage finds 971 frames before the first acceptance at 91.234 s: 109 B-feature failures, 7 A-feature failures, 812 precheck rejects and 43 actual stability evaluations without acceptance. A-correlation failures involve 673 frames, G failures 496 (overlapping). At 41.052–43.863 s, 11 precheck-passing candidates at lagB 128–129 still do not meet 14-candidate warmup after the preceding reset. Investigate A/G feature selection and intermittent-gate/stability interaction, not merely whole-round acceptance. These are local causal code-path observations, not proof of the cause on an unrecorded difficult subject. Evidence and revised sequence: `docs/research/measurement-gate-audit-20260905/self-trial-20260905.md`. Product code and deployment remain unchanged.

Offline comparison approved by the user is now verified and documented. Proposed first independent trial: round A .80 to .78, retaining frame A .78 and all other rules. It removes three whole-round quality failures in this recording but changes the conditional final SOS from 3910.47 to 3938.38; it does not solve the initial 95-second positioning delay. G +/-8 is a separate modest candidate: last two rounds finish about .57/1.22 seconds earlier on the recorded trajectory, final 3909.11, but initial delay and three quality failures remain. Lowering frame A alone to .75 reduces quality-passing rounds from five to four; larger G ranges admit additional high-SOS candidates and cannot be justified by the similar aggregate alone. Findings are conditional replay, not accuracy or prospective speed proof. No threshold deployment is authorized. Full rationale and limitations: `docs/research/measurement-gate-audit-20260905/offline-comparison.md`.

The user supplied the actual `build/debug/debug/measurement-experiments/` directory. Eight complete start/stop logs (1865 four-channel frames; implementation marker v1, build 20:44:34) resolve the missing-file report; do not continue asking for the launch path or assume the earlier stale-executable hypothesis was proven. Three attempts reach 30 valid samples but fail the stricter round A-correlation floor (about .789 versus .80, while frame floor is .78). Last two attempts have 21 and 27 G-only rejected frames whose computed SOS overlaps accepted frames (3889–3920), but earlier G-only rejected values span 3379–4336. Correct final SOS reconstruction is 3910.47; the previously reported 3898.11 wrongly trimmed a five-value set and is superseded. User says no contemporary sample-machine reading, only recollection of about 3926. This is not paired accuracy evidence. Focused human analysis: `docs/research/measurement-gate-audit-20260905/self-trial-20260905.md`. No product or parameter changes in this follow-up.

Follow-up: the user reports measuring once with the new program but finding no `measurement-experiments` folder. Read-only inspection found no running BoneDensity process, and no experiment files in the actual product build directories (only synthetic test logs). Of four workspace executables inspected, only `build/debug/debug/BoneDensity.exe` (2026-09-05 19:46:01) contains the new recording path and implementation marker; the Qt Creator output `build/Desktop_Qt_6_5_3_MinGW_64_bit_Debug/debug/BoneDensity.exe` is from 00:40:52 that day and lacks them, while `build/debug/BoneDensity.exe` is from July. This establishes multiple stale launch candidates, not which one the user actually used. Confirm their executable path and that acquisition used the patient `开始检测` flow before changing product code or asking for another measurement. Existing staged `.gitignore` is user work and must remain untouched.

2026-09-05 continuation approval authorizes the narrow state repairs and experiment capture described above. Earlier statements below that no production repair is approved are historical audit context and are superseded only for this extension. No wider D/G/correlation threshold, feature redefinition, or SOS formula change is selected. First self-trial is the user's chosen acceptance order; the user need not diagnose rejection causes or manually time acquisition.

On 2026-09-05 the user requested evidence-backed algorithm improvements and provided an algorithm DOCX plus six CSV attachments (five unique hashes). They explicitly confirmed these are only one subject at different angles, with no paired reference readings or hard-subject logs. The existing tutorial contract and approved plan remain applicable only to that earlier work; the new request authorizes inspection, offline analysis, and diagnostic reproduction, not an invented final threshold package.

The audit found: stale candidate/bar state across invalid input; retained accepted values across cluster changes, demonstrated by existing C++ accepting a 3791.67 round formed from 4083.33 and 3500 clusters; asymmetric envelope/valley G definitions; and speed-dependent correlation-pick bias on exactly delayed identical synthetic waveforms. Historical CSV sensitivity shows that increasing G tolerance increases both reference-region and high-SOS rows. The 3500-named file contains 6058 mixed-mode/mixed-output rows and cannot serve as a homogeneous label or continuous replay. These are reproducible code/data findings, not evidence of improved clinical accuracy or the specific cause on an unrecorded subject.

Evidence and concrete next-change recommendations: `docs/research/measurement-gate-audit-20260905/README.md`, `results.json`, and `code-results.json`. Product source files and the deployed executable were not modified by this audit. Continue by specifying full-path experiment recording and stale/mixed-state repair, then comparing consistent-feature and local-correlation candidates against raw recordings. Exact threshold selection and hard-subject acceptance need new paired recordings. Do not overwrite the existing tutorial milestone or mark it accepted on the basis of this audit.

## Verification Status

| Criterion | Status | Result / evidence |
| --- | --- | --- |
| `SC-17` | passed | Nine original raw/feature baselines and actual dual0.78 raw/cached candidates match; per-frame A/B/D/G/SOS checked. Independent dual0.80/control results unchanged. Common-lag, both-window, invalid-input, A/B/G round rejection and three-profile regressions pass. See dual-window-078-implementation.md. |
| `SC-18` | passed (software artifact); hardware pending under SC-14 | Independent dual trial built/deployed, title/profile verified offscreen, runtime imports resolve. Original/previous exe hashes unchanged and input hashes verified. No clinical XML in new runtime. No hardware or clinical acceptance claimed. |
| `SC-15` | passed | Default baseline and research scenarios identical; all eight actual raw experimental checks match. Both build profiles pass G-only exclusion and basic/cluster-loss regressions. Formulas, numeric thresholds and bar mappings unchanged. See self-trial-implementation.md. |
| `SC-16` | passed | Separate fixed-toolchain Debug artifact deployed; tests assert distinct title/log marker. Original executable hash unchanged; input manifest verified and no clinical XML copied. This covers software isolation, not hardware benefit, which remains pending under SC-14. |
| `SC-1` | passed | Original bar geometry/mapping and numerical formulas/thresholds/timing remain unchanged. Approved exception: patient stale/mixed state and unavailable-reading indication are verified under SC-12, not claimed behavior-identical. |
| `SC-2` | passed | Stable fixed instruction and round-level status remain unchanged across valid and invalid incoming frames; effective-frame progress still advances. |
| `SC-3` | passed | Tests verify exactly three pages and a separate manual dialog; the entry is disabled during acquisition/debug and manual close cannot start acquisition. |
| `SC-4` | passed | Isolated tests cover current-version marker write/read, automatic skip, cancel-without-marker, and start/no-start behavior without clinical XML changes. |
| `SC-5` | passed | Qt screenshots verify text-free temporary drawings; optional page-specific resource paths allow later image replacement without flow changes. |
| `SC-6` | passed | A focused test starts round 2 with Space on the existing button; no global shortcut or page-click handler was added. |
| `SC-7` | passed | All focused/existing suites, canonical Debug build, Qt screenshot inspection, `git diff --check`, and protected-file scope checks passed. |
| `SC-8` | pending | Requires user visual acceptance and later real-device usability confirmation. |
| `SC-9` | passed | Existing patient-store evidence covers all seven semantic-corruption cases and source/list preservation. |
| `SC-10` | passed | Existing patient-store evidence covers valid version 2, legacy migration, paired operations, and transaction recovery. |
| `SC-11` | passed | Main-window 35, patient 23, account 6, calibration 16, portable-handoff, and canonical Debug build all passed. |
| `SC-12` | passed | Three before-failing/after-passing cases plus actual invalid-wave pipeline; all seven lists clear on loss, brief dropout survives, uninterrupted 43-input/30-valid sequence preserved. |
| `SC-13` | passed | Early failures, actual features, full round order, raw encoding, unique paths, cap/failure isolation and automatic Debug/disabled Release verified. Incomplete serial packets and hardware/storage truth remain documented limits. |
| `SC-14` | failed (positioning); accuracy pending | Prior software/build evidence remains valid. New self-trial yields zero valid samples over171.488s; requested positioning benefit not achieved in this attempt. A-correlation and pre-warmup resets identified from log, not physical cause. No paired accuracy evidence. See self-trial-no-progress-20260906.md. |

## Next Actions

`User self-trial of build/self-trial-dual-window/debug/BoneDensity_DualWindowTrial.exe: two independently positioned measurements; assess easier progress/stalls and retain all automatic logs including failures. Do not force continuation on prolonged stalls or select only near3900 results. Paired accuracy/other-subject evaluation remains pending. No commit/push authorized.`

## Iteration Control

- Active Failure: `SC-14 positioning: supplied experimental self-trial has zero accepted frames over171.488s`
- Meaningful Iterations: `2`
- New Evidence: `Approved dual-window0.78 implementation now passes the failed-log round in raw replay, but first wait is77.711s and actual hardware benefit is pending. Do not clear SC14 based on replay.`

## Blockers

`none`
