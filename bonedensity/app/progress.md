# 实施进度

## 2026-07-10：阶段 A

- 已创建 `codex/feature-complete` 分支。
- 基线 Qt Debug 构建通过。
- 已确认：现有工作区包含未提交的 USB 热拔插稳定性修改；本轮保留该修改，不做回退。
- 硬件、串口和真实测量结果尚未进行现场验证。

## 2026-07-10：阶段 B

- 新增 `PatientStore`：患者档案写入 `patients.xml`（version 2），检测历史使用独立 `measurements.xml`。
- 旧版 `patients.xml` 在首次迁移前复制为 `patients.xml.bak`；迁移或写入失败会保留原文件。
- XML 使用 `QSaveFile` 原子写入。
- Qt Debug 构建通过；旧 XML 实际迁移、真实历史数据仍未验证。

## 2026-07-10：阶段 C

- `btnPatientInfo` 仅负责建立档案/更换患者；新增 `btnStartMeasurement` 承担原有开始/停止检测流程。
- 增加患者选择 UI 状态更新和统一切换入口；新建或导入患者只回主界面，不自动检测。
- 新建患者会立即保存基本档案；切换患者清空未完成轮次、趋势和临时显示，不影响历史文件。
- Qt Debug 构建通过；真实串口检测、手动停止与热拔插组合行为需要实验室现场验证。

## 2026-07-10：阶段 D

- 完整检测后生成待保存结果；“保存结果”追加到 `measurements.xml`，同一份待保存结果只能保存一次。
- 增加当前患者的检测历史窗口，可删除单条检测记录；删除患者会同步删除该患者历史。
- Qt Debug 构建通过；完整硬件检测产生结果后的保存流程需要实验室现场验证。

## 2026-07-10：阶段 E/F

- 新增 `accounts.xml`：首次启动生成管理员，密码以随机盐加 SHA-256 哈希保存。
- 管理员可在菜单“账号管理”中创建、启用/停用、重置密码和删除普通账号；普通账号没有入口。
- 最终 Qt Debug 构建通过。界面交互、文件持久化、旧 XML 实际迁移和全部硬件检测流程均未在本机运行验证，需现场验证。

## 2026-07-10：阶段 G（审查问题定向修复）

- 旧 `patients.xml` 迁移改为完整读取并关闭源文件后写入；保留 `.bak`，与已有 `measurements.xml` 合并后按患者、时间、部位、SOS、诊断去重。迁移失败不清空调用方内存，重试不会重复生成记录。
- 患者新建、编辑、删除、单条历史删除和账号变更均先保存候选数据，成功后再更新内存和界面；患者级联删除先写检测记录，患者文件失败时尝试恢复原记录，避免孤儿记录。
- 删除当前患者后清空选择、待保存结果和检测临时状态；检测运行中禁止档案进入、编辑、删除和切换。档案最新日期/SOS 与日期搜索均从 `MeasurementRecord` 派生。
- 空、损坏或无启用管理员的 `accounts.xml` 会备份为 `.bad.bak` 后恢复 `admin / 1`；账号名先去空格再查重。
- 已回退 `readyRead` 连接位置、断开复位和 `ResourceError` 处理的本轮串口生命周期改动。串口协议、115200 波特率、80 ms 采集时序和信号处理逻辑未改动。
- 实测：Debug 构建通过；旧 XML 首次迁移得到 1 名患者和 2 条不同历史，重复旧记录仅迁移一次，重启后历史文件不变；空账号文件恢复和 `alice`/` alice ` 重名检查通过。
- 未验证：GUI 点击式的新建/编辑/删除患者、删除当前患者后按钮状态、目录不可写时的完整 UI 提示，以及全部硬件、串口和热拔插流程，均需要现场验证。

## 2026-07-11：阶段 H（最终阻塞问题收尾）

- 管理员有效性增加用户名非空、盐 Base64 解码非空、密码哈希非空及 SHA-256 解码长度检查；无有效启用管理员时备份异常 XML 并恢复 `admin / 1`。
- 患者新建和详情表单隐藏检查日期、诊断旧控件，保存代码只读取姓名、ID、性别、生日、身高、体重。
- 实际运行账号恢复测试：空盐、非法盐、空哈希、错误哈希长度均产生备份并恢复可认证的 `admin / 1`；正常账号文件重新加载未产生备份。
- 实际运行患者存储测试：基本档案保存后重载一致；历史检测时间、SOS、诊断保存后重载一致。代码检查确认历史表仍展示这些字段。
- 最终 Debug clean build 通过。未进行 GUI 人工点击和重启软件操作；真实硬件检测、串口及热拔插仍需实验室现场验证。

## 2026-07-11：阶段 I（严格解码与表单只读收尾）

- 管理员凭据改用 `QByteArray::fromBase64Encoding()` 与 `AbortOnBase64DecodingErrors` 严格解码；解码失败、空盐、空哈希或错误 SHA-256 长度均触发既有备份与 `admin / 1` 恢复。
- 实际测试通过：`QUJD!!!!`、纯非法 salt、空 salt、空 hash、错误 hash 长度均备份并恢复可认证管理员；正常账号文件不被重置。
- `dID` 设为只读，新增患者 ID 仍保持可编辑；检查日期和诊断的输入框及残留标签均隐藏。历史窗口仍通过代码保留检测时间、SOS、诊断列。
- 最终 Debug clean build 通过。详情页/新建页的人工 GUI 点击和真实硬件流程仍需人工或实验室验证。

## 2026-07-11：组会汇报材料

- 新增 `GROUP_MEETING_UPDATE.md`，将本次工作分为“功能更新”和“问题修复”两部分，面向非技术听众说明目的、做法和使用效果。
- 文档包含可直接用于组会的口语汇报稿，并明确已完成的程序测试与仍需实验室验证的内容。

## 2026-07-12：阶段 J（检测完成与档案交互调整）

- 五轮检测完成后自动追加检测记录，提示“结果已自动保存，可在档案中查看”。
- 同一患者再次点击开始检测时重置本轮临时状态，不再要求进入不存在的报表页面或更换患者。
- 移除主检测页面的检测历史按钮；档案增加“查看历史”入口，继续一人一行，日期和 SOS 派生自该患者最新历史记录。
- 档案导入时显示“选择患者”按钮和操作提示，勾选或选中患者后点击按钮为主要方式，双击导入继续保留。
- Qt Debug 构建通过；本轮尚未连接真实设备完成五轮自动保存和重复测量现场验证。

## 2026-07-12：阶段 K（USB 断开提示与界面中文化）

- USB 拔出时增加一次性处理标记，先停止自动采集、清理接收缓存和通道状态，再关闭串口并恢复“连接”按钮，避免重复错误事件继续堆积。
- 错误提示改为“设备已断开”，说明可能原因是 USB 被拔出或连接不稳定，并提示检查 USB 线、设备电源后重新连接。
- 连接成功后的 `readyRead` 使用唯一连接，避免多次连接后重复处理数据；连接、断开连接、获取波形等用户可见文字改为中文。
- 已完成代码检查；真实 USB 热拔插、不同设备驱动错误和医生现场阅读体验仍需实验室验证。

## 2026-07-12：阶段 L（小界面交互修正）

- 将左侧单帧采集按钮改为“获取一帧”，右侧原 trigger 自动采集按钮改为“自动采集”，运行中显示“停止自动采集”。
- 导入患者时隐藏“查看历史”，导入流程只负责选择或添加患者；普通档案页面保留查看历史功能。
- 将主检测页的患者操作按钮居中成组，并将新档案页底部的确定、导入、返回按钮上移、等宽排列，减少底部被截断和上下布局不均的问题。
- 已完成代码级检查；不同屏幕分辨率下的实际视觉效果仍需人工确认。
- 主界面增加窗口尺寸同步：工具栏、主体区域、波形图、趋势图和右侧结果/示意图随可用区域重新布置，减少固定高度导致的底部和右下角遮挡。
- Debug 构建通过；仍需在实验室电脑的实际分辨率和窗口状态下人工确认显示效果。
