# 项目进度

本文件面向导师和项目成员，按周从近到远记录重要成果，保持直白简短。默认只写已完成并验证的内容；确实尚未验收的事项必须明确标记。维护规则见 `AGENTS.md`。

## 2026-07-13 至 2026-07-19

### 本周任务与完成情况

- 完成正式检测报表，可从检测历史查看，并支持导出A4 PDF和打开打印窗口。
- 完成年龄-SOS参考图，可按患者性别和年龄切换背景，并标出最新一次检测位置（待用户验收）。
- 完成标准试块探头校准的软件流程，包括资料登记、重复采集、独立验证、参数保存和恢复；真实设备校准待实验室验证。
- 固定Qt命令行构建环境，解决MSYS2编译器混用造成的程序入口点错误。
- 统一项目需求、任务、上下文和周报文档，防止长对话后需求和进度丢失。

### 调研依据

- 调研了临床文档的PDF使用方式，参考DICOM对封装PDF的支持。本项目目前只提供普通PDF导出，不宣称已经接入医院信息系统。
- 报表版式使用课题组提供的样机素材，结果字段只取程序真实产生的数据。
- 年龄-SOS图使用课题组提供的四张统计参考图，并逐张核对年龄、SOS刻度和绘图区。
- 校准流程主要参考 `YY/T 0774-2019` 中不少于6次测量、SOS误差 `±2%`、重复性 `1%` 和温度偏差 `±1 C` 的要求。具体条款和官方链接见 [探头校准资料依据](../research/probe-calibration-research.md)。

### 更新功能及实际做法

- 为了保存和回看完整检测结果，检测记录同时保存患者资料快照，并用同一套绘制逻辑显示、导出和打印。优点是旧报表不会随患者资料修改而变化；局限是实体打印效果仍受打印机驱动影响。
- 为了避免反复出现编译器入口错误，构建脚本固定Qt 6.5.3和MinGW 11.2。优点是结果稳定；局限是其他电脑需要相同安装路径或调整脚本。
- 报表页面、PDF和打印窗口已验证；真实设备五轮完成后的自动跳转和实体出纸尚未在实验室验收。
- 年龄-SOS图只显示最新真实记录，无记录或数据超范围时不画假点。优点是结果直观且不改检测算法；局限是参考范围完全取决于现有素材。
- 为了在不影响现有检测的前提下校准探头，软件先生成候选D，再用重新涂耦合剂后的6次独立测量验证，最后由操作者确认是否启用。优点是失败或中途退出不会改变患者测量参数；局限是温度、耦合和证书真实性仍需操作者负责。

### 关键验收证据与复现

- 匿名测试数据保存在 `testdata/age-sos-reference/`。在独立程序目录使用这两份XML，依次选择 `G19/W20` 可演示19/20岁分界，选择 `M45` 可演示只取最新记录，选择 `EMPTY/OUTSIDE` 可演示无结果和超范围时不画假点。
- 四类人群、边界年龄、旧记录回算、无记录和超范围均已由Agent实际运行验证；本功能仍待用户验收。
- 运行 `test-calibration.ps1` 可复现校准公式、阈值边界、6+6次流程、参数不提前生效、XML损坏恢复和研发模式不可激活等关键场景；完整程序的校准说明页和资料页已实际启动检查。

### 下一步

- 在实验室使用合格试块和外部温度计验收真实校准采集；之后审计整机数据迁移。

## 2026-07-06 至 2026-07-12

### 本周任务与完成情况

- 增加本地账号管理，管理员可管理普通账号，密码不再明文保存。
- 将患者档案与检测历史分开，同一患者可以保留多次检测。
- 将建立/更换患者与开始/停止检测分开，切换患者时清空未完成数据。
- 优化档案导入、检测自动保存、USB断开提示、中文按钮和主界面布局。

### 更新功能及实际做法

- 为了避免新结果覆盖旧结果，患者资料和检测记录分别保存为XML。优点是历史清晰并兼容旧数据；局限是整机换电脑迁移仍需专门功能。
- 为了避免USB拔出后连续弹窗，异常只处理一次并停止采集、清理缓存。真实热拔插与重连仍需实验室验证。

## 2026-06-29 至 2026-07-05

### 本周任务与完成情况

- 集中管理测量配置，并根据试块测试结果暂时采用B通道作为正式患者SOS来源。

### 更新功能及实际做法

- B通道与参考样机更接近，暂时优先使用B通道以提高结果稳定性。局限是A通道硬件偏差尚未通过标准试块校准解决。

## 2026-06-22 至 2026-06-28

### 本周任务与完成情况

- 将信号处理、统计和骨健康计算从主窗口中拆出，并增加检测门控诊断信息。

### 更新功能及实际做法

- 这次调整主要为了让代码更容易定位问题，没有改变原有检测门限和经验参数。门控诊断可以统计有效帧被哪一步拦截，便于实验室排查。
