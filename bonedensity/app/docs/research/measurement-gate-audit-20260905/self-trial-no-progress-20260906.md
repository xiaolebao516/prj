# 独立试测失败记录：两个条达标但没有有效值

用户反馈：本人在试测版中也遇到之前难测同学的情况，感觉位置已经不错，但有效值一直不推进。本次负反馈不支持把该实验发布为已改善定位的版本。

输入为 `build/self-trial-observe-g/debug/measurement-experiments/round-20260906-082027-716-d776c5ad-b4ca-44a1-b4f0-205bdd1e46e1.jsonl`，SHA-256 `3e203e9db620c4dbc23e90503af2a00ea2965ad2f305f061db40e194a70d3c2b`。记录完整，序号连续；标记确为 `observe-before-g-20260906-v1`，探头基线0.00784m、各门槛与批准的试测配置一致。

## 已确认的事实

- 从开始至停止171.488秒，共1827帧，没有任何有效值、稳定锁定或完成轮。163次清理事件每次均丢弃0个有效值，不能解释成“测出的值被删除”。
- 316帧在完整门控前退出：B特征无效283、A特征无效29、A/B差异3、B跳变1。其余1511帧中A相关性失败1392次（92.1%）；G失败849次、D失败185次、B相关性失败107次。失败条件有重叠，不能相加。
- D/G同时达标645帧，其中630帧A相关性不达标；598帧除了A相关性以外，所有其他前置检查均通过。这证明“两个条达标但无法推进”确实存在，不是用户必须承认自己没对准。达标不等于恰好在中线，也不证明物理姿势或SOS正确。
- 598帧的A相关性中位数0.62497，原要求0.78；只有11帧达到0.75。轻微降低该门槛只能挽回少数前置机会，不能直接推断能完成整轮。
- 实验顺序累计允许75帧进入稳定观察，全部前置条件同时通过仅15帧。按现有连续10帧拒绝则重置规则，两次重置之间最多观察12帧，未达到14帧预热。因此全过程无稳定锁定、无有效值。
- 同一批已记录特征按默认前置顺序计数，重置间最多5帧，也达不到14。这是进入首次锁定之前的逻辑计数，不是原始波形回放；它不能比较两次实际操作，也不能断言试测版导致或没有导致整体体验退化。
- 帧间隔中位94ms、最大121ms；记录的单帧处理时间中位3ms、最大12ms。记录覆盖区间没有秒级数据中断，现有证据指向判定未通过而非算力卡死，不等于完成硬件/串口验收。

## 代码对应与判断边界

`src/mainwindow.cpp` 的 `updateProcessPanel` 只把D/G映射到两个条；不包含相关性、预热状态。`detectAndPlotSpeed` 中 `corrOk` 同时要求A/B相关性通过，且仍在实验版稳定观察之前。`checkBoneLagStable` 与 `rejectBoneLagCandidate` 的预热/重置交互解释本次零进度。

本次没有重新计算原始波形，没有证明低相关性一定是接触不好、器件问题、真实姿势错误或算法错误；门控统计不能作物理因果结论。当前主要线索从G观察顺序转向A通道特征/相关性。优先利用现有失败原波形与之前可完成的记录检查A取点、取波段与相关性选择，区分信号本身差异和评价方法不适配，再提出有依据的候选。暂不改公式/参数，不建议把0.78直接降到0.62，也不要求用户立即重测或手动诊断。

## 复现与范围

只读运行 `inspect_no_progress.py <上述日志路径>`，输出关键计数并核对记录完整性、版本和前置判定一致性。脚本只适用于从未锁定/接受的当前实验记录；不冒充完整回放，不改输入，不保存患者信息。已实际运行通过，与直接统计一致。

本次仅增加诊断脚本、本说明和当前State；未修改/构建/覆盖程序，未操作桌面或设备，未改用户已暂存文件，未提交或推送Git。
