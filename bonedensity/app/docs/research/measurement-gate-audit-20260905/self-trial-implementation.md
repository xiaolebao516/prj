# 姿态流程独立试测版：实现与验证

2026-09-06，用户在具体方案与限制说明后批准尝试。目标是验证更早出现有效进度是否帮助操作者保持姿势，不是保证提速或扩大允许误差。

## 实际改动

只在显式实验构建中，基础信号和D合格时提前观察稳定性，不必等G同时合格。真正计入有效值仍须当帧G及其他原条件全部通过。G单独不合格不再打断这部分观察，但真正失去信号或改变稳定簇仍清除旧的部分轮进度。

这是判定顺序改变，不能称为算法完全没变。SOS、D/G公式、全部数值门槛、30点/五轮汇总、采集时序和进度条映射未改。默认构建保留原顺序。

## 自动验证

- 测试先行：新增真实合成波形回归在改动前出现预期失败，改动后通过。
- 原版与实验版完整主窗口回归各45通过、0失败、1可选截图跳过。验证G不合格不计数、换簇/失去信号清空、标题与记录版本等。结果分别在 `build/tests/mainwindow-safety/after-observe.xml`、`build/tests/self-trial-safety/trial-safety.xml`；改动前结果为同原版测试目录下 `before-observe.xml`。
- `implemented-observe-before-g-results.json`：1865帧原版基线与之前完全一致；八份记录的实验源码原始波形处理均匹配独立研究回放，研究候选结果也完全一致。输入清单带SHA-256。
- 固定Qt 6.5.3/MinGW 11.2独立编译、部署成功。可用项目根目录 `build-self-trial.ps1` 重建，不覆盖正常构建。未启动交付目录程序、操控桌面或连接设备，不能宣称实机启动/采集验收。
- 原版 `build/debug/debug/BoneDensity.exe` SHA-256仍为 `661043a6578cffb9a54975077b208c5f8c092f2dc4e2fa2f275b66f844a584ce`。独立运行目录没有复制账户、患者、测量或校准XML。
- 八份原始记录的当前文件哈希均与清单匹配；所有程序及插件引用的Qt/MinGW运行库均已部署。`git diff --check`通过（仅换行格式提示）。

## 程序和复现

试测程序：`build/self-trial-observe-g/debug/BoneDensity_SelfTrial.exe`，SHA-256为 `62f5d12918e29c8fd4fefcf97228e4895310b4f18b94e1ff67991f08d91bc96f`。

标题包含“姿态流程试测版（仅研发验证）”；Debug记录版本为 `observe-before-g-20260906-v1`。启动后按原有首次使用流程添加自己的测试档案，正常操作即可；使用说明随程序放在 `README.txt`。返回记录位于该程序旁的 `measurement-experiments`。新记录版本须按新流程解释，不能冒充旧版本回放。

## 必须保留的限制

固定记录中首次接受91.234秒→41.052秒，但新增11个值后来被丢弃；首次攒满94.795秒→94.604秒，几乎没变。八次尝试的整轮质量通过由5次降为4次。观察顺序变更不是已经证明有效的优化。

下一步由用户普通自测两次，观察进度出现后保持姿势能否持续推进；保留失败/中断记录，不需要手工判断拒绝原因或精确计时。之后再考虑难测同学和同期样机对比。不能从单人旧记录或回忆中的3926推断准确性，实验结果不用于诊断。未提交或推送Git。
